version: "3.9"

services:
  giftbot:
    build: .
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./giftbot.db:/app/giftbot.db
      - ./uploads:/app/uploads
